import NoneStyle from "@/lib/layout";
import { convertMetadata } from "@/lib/metadata";
import { toolmetas } from "@/text/meta";

export const metadata = convertMetadata(toolmetas["calc"]);
export default NoneStyle;
